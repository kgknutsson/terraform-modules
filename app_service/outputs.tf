output "application_hostname" {
  value       = "https://${var.is_function ? azurerm_function_app.this.0.default_hostname : azurerm_app_service.this.0.default_site_hostname}"
  description = "The web application URL."
}

output "application_caf_name" {
  value       = azurecaf_name.app_service.result
  description = "The application name generated by the Azure Cloud Adoption Framework."
}

// TODO: Jag hade haft fler outputs för tydlighetensskull så man lätt kan sanity checka vad som hänt, framförallt så kan du upptäcka om någon modul ändra betende när den stegar upp version. Om det till exempel är jätte viktigt att en app service är stängd från internet access så skriv ut den "variabel" output. Då ser ni när resusen skapas att de är korrekt, Om den sedan ändrar på sig så ser ni de snabbt eller kan ni lägga ett automatiserat larm.